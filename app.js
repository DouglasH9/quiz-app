//question object arrayvar qArray = [];//var to keep track and update current question setvar currentQSet = 0;//question and answer set constructor	function Questionset(question, answerArray, correctAns){		this.question = question;		this.ansArray= answerArray;		this.correctAns= correctAns;	};	//question and answer set objects	qArray[0] = new Questionset("Have you ever heard the joke about the pizza?",['no.',"yes, it's terrible.", 'no? Eh,you wouldn\'t like it anyway, it\'s too cheesy.','this quiz is stupid.'], 2);	qArray[1] = new Questionset('So I went shopping for camouflage pants the other day...', ['and I couldn\'t find any', 'and I got recruited by the army','pants are expensive!', 'these jokes are painful.'], 0);	qArray[2] = new Questionset("I used to be addicted to soap...", ['and boy did that lava bar burn my skin!', 'where are you getting these dumb jokes?!?', 'and my life went down the drain', 'but it\'s ok though, cuz I\'m clean now.'], 3);$(document).ready(function() {	//var that counts correct answers	var numCorrect = 0;	//keeps track of question	var qCount = 0;		//selects item from answer choices	$(document).on('click','li', function(){		if ($(this).closest('li').hasClass('choice')){			$('li').removeClass('chosen');			$('li').addClass('choice');			$(this).closest('li').removeClass('choice');			$(this).closest('li').addClass('chosen');		}			else if ($(this).closest('li').hasClass('chosen')){				$(this).closest('li').addClass('choice');				$(this).closest('li').removeClass('chosen');			}	});		//starts game when start button is clicked	$('#startButton').click(function(){		console.log('click');		$('#instructionBox').fadeOut('fast');		$('#qBox').fadeIn('fast');		$('#question').html(qArray[currentQSet].question);		$('#ansOne').html(qArray[currentQSet].ansArray[0]);		$('#ansTwo').html(qArray[currentQSet].ansArray[1]);		$('#ansThree').html(qArray[currentQSet].ansArray[2]);		$('#ansFour').html(qArray[currentQSet].ansArray[3]);	});	$('#subBut').click(function(){		if ($('li.chosen').html()==qArray[currentQSet].ansArray[qArray[currentQSet].correctAns]){				numCorrect++;								console.log(numCorrect +' out of 7 correct');			}				else{					console.log('wrong choice!');				}		currentQSet++;		$('#question').html(qArray[currentQSet].question);		$('#ansOne').html(qArray[currentQSet].ansArray[0]);		$('#ansTwo').html(qArray[currentQSet].ansArray[1]);		$('#ansThree').html(qArray[currentQSet].ansArray[2]);		$('#ansFour').html(qArray[currentQSet].ansArray[3]);		$('li').removeClass('chosen');		$('li').addClass('choice');	});		});